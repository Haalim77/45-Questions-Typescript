var userName = ["Haalim", "Admin", "Nimra", "Alia", "climber"];
//empty array
userName = [];
//show empty array message
if (userName.length === 0) {
    console.log("Your Array is Empty we need to find some users!");
}
